{% block body %}
	<div
		class="card border-0 shadow rounded-3 my-5 bg-secondary">
		<div
			class="card-body ">
			{{ form_start(uapForm, { attr: { class: 'd-flex flex-column ', id: 'formCreationForm', method: 'POST', enctype: 'multipart/form-data' }, action: path('app_project_creation') }) }}
			<div
				class="col-md-auto">
				{{ form_label(uapForm.name) }}
				{{ form_widget(uapForm.name) }}
			</div>
			<div
				class="d-grid col-md-auto mx-auto">
				{{ form_label(uapForm.save) }}
				{{ form_widget(uapForm.save) }}
			</div>
			{{ form_end(uapForm) }}
		</div>
	</div>
{% endblock %}
